const borderTop = {
    borderTopColor: 'transparent'
}

const Loading = () => (
    <div className="w-8 h-8 border-4 border-black border-double rounded-full mx-auto animate-spin" style={borderTop} />
);

export default Loading;
